# Copy this file to android/keystore.properties and keep the real file private.
# Prefer an absolute storeFile path so builds do not depend on the working directory.
storeFile=/absolute/path/to/airplanemode-upload.jks
storePassword=replace-me
keyAlias=upload
keyPassword=replace-me
